name: yapayzeka
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dash_chat_2: ^0.0.21
  flutter_gemini: ^2.0.4 # Bu sürüm mevcut değilse, doğru sürümü kontrol edin ve güncelleyin

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
